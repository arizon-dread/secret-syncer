apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: handle-secrets
  namespace: YOUR-NAMESPACE
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["get", "create", "update"]
