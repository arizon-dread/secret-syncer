apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: handle-secrets
  namespace: poolen-secret-syncer-tests
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["get", "create", "update"]
